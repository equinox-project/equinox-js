{
  "name": "equinox-js",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@equinox-js/dynamo-store-cli": "workspace:*",
    "@equinox-js/explorer": "workspace:*",
    "@vitest/coverage-v8": "^1.0.4",
    "prettier": "latest",
    "ts-node": "^10.9.1",
    "turbo": "latest",
    "vitest": "^1.0.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@8.7.0",
  "dependencies": {
    "typescript": "^5.3.3"
  }
}
